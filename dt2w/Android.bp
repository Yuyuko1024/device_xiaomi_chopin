android_app {
    // the build system in pi-dev can't quite handle R.java in kt
    // so we will have a mix of java and kotlin files
    srcs: ["src/**/*.java", "src/**/*.kt"],
    resource_dirs: ["res"],
    name: "DT2W-Service-MT6891",
    certificate: "platform",
    platform_apis: true,
    optimize: {
        enabled: false,
    },
    privileged: true

    required: [
        "privapp-permissions_co.potatoproject.dt2w.MT6891"
    ],
}

prebuilt_etc {
    name: "privapp-permissions_co.potatoproject.dt2w.MT6891",
    sub_dir: "permissions",
    src: "privapp-permissions_co.potatoproject.dt2w.MT6891.xml",
    filename_from_src: true,
}
